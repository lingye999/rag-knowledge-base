## 下一步工作计划

### 整体路线

围绕设计文档的 Day 3 检索引擎，分两大阶段：

**Phase A：存储基础设施升级**（你手写，我辅助）
**Phase B：检索引擎功能**（Day 3.2 - 3.5）

Phase A 是 B 的前提条件——没有 doc_registry 和元数据，就做不了 Route B 文档级召回和三维度加权。

---

### Phase A：存储基础设施升级

你手动写，我帮你做设计验证和调试。改动范围：

**A1. base.py — 加三个核心数据结构**
```
doc_registry: dict[str, list[int]]    ← 文档名 → FAISS 位置列表
meta:        list[dict]               ← 每个位置的元信息（来源文档、页数等）
deleted:     set[int]                 ← 已删除的位置集合
```

**A2. base.py — 改 add_batch 签名**
```python
def add_batch(self, texts, vectors, doc_name=None):
```
传入 doc_name 时自动维护 doc_registry 和 meta。不传则兼容旧用法。

**A3. 新增方法（你手写核心逻辑）**
- `delete_doc(doc_name)` → 标记删除整个文档的位置
- `_compact()` → 重建：抽活向量 → 建新 FAISS → 重映射 doc_registry
- `get_chunks_by_doc(doc_name)` → 按文档取所有 chunk

**A4. cli.py 联动**
- `/add` 时传入文件名作为 doc_name
- 新增 `/delete <doc_name>` 命令
- `/save` 时自动 compact

---

### Phase B：检索引擎功能（接在 Phase A 之后）

| 步骤 | 功能 | 涉及文件 | 前置依赖 |
|------|------|---------|---------|
| B1 Self-Querying | LLM 从自然语言提取 (query, filters) | llm_service.py | 无 |
| B2 Route A+B 并行召回 | 切片级 + 文档级双路 | 新建 retriever.py | Phase A doc_registry |
| B3 Phase 2 定向召回 | doc_id 范围内二次搜切片 | retriever.py | B2 |
| B4 三维度加权 | α0.7 + β0.1 + γ0.2 | retriever.py | B2 |
| B5 Threshold 过滤 | finalScore < 0.3 丢弃 | cli.py + retriever.py | B4 |

---

### 今天先做什么？

从 Phase A 开始，如果你同意这个顺序，我们就从 A1 动手。流程是：
1. 我描述要加的数据结构和接口
2. 你自己在 base.py 里写代码
3. 写完我们一起检查、验证